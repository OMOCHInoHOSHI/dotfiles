# dotfiles
再読み込みのコマンド
`source ~/.bashrc`

## opencode設定 (オプション)
opencode を使うデバイスでは、リポジトリをクローンしてセットアップしてください。

```bash
git clone git@github.com:OMOCHInoHOSHI/dotfiles-opencode.git ~/dotfiles-opencode
```

詳細は `dotfiles-opencode/README.md` を参照してください。
